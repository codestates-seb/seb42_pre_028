version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build Starting on `date`
      - cd DeployServer
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo $(basename ./seb42_pre_28/build/libs/*.jar)
artifacts:
  files:
    - seb42_pre_28/build/libs/*.jar
    - seb42_pre_28/scripts/**
    - seb42_pre_28/appspec.yml
  discard-paths: yes